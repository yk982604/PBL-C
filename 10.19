1.pythonに接続されたカメラが撮影する。
2.撮影した写真から判断してその画像に人が含まれるかどうか判別する。
3.写真に含まれる人数が2人以上の場合、ブザーが鳴る。

import RPi.GPIO as GPIO
import time

GPIO.setmode (GPIO.BCM)
GPIO.setup (3, GPIO.OUT)

camlog3.txt >= 2

while True:
  pwm = GPIO.PWM(3.100)
  pwm.start(100)
  time.sleep(4)
  pwm.stop()
  break
　else:
 　　 break
    time.sleep (0.01)
 
GPIO.cleanup ()
